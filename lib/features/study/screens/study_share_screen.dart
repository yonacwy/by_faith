import 'package:flutter/material.dart';

class StudyShareScreen extends StatelessWidget {
  const StudyShareScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Study Share Screen'),
      ),
    );
  }
}