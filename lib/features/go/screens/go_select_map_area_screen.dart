import 'package:flutter/material.dart';

class GoSelectMapAreaScreen extends StatelessWidget {
  const GoSelectMapAreaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Select Map Area'),
      ),
      body: const Center(
        child: Text('Select Map Area Screen Content'),
      ),
    );
  }
}