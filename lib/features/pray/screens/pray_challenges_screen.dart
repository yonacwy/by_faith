import 'package:flutter/material.dart';

class PrayChallengesScreen extends StatelessWidget {
  const PrayChallengesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pray Challenges'),
      ),
      body: const Center(
        child: Text('Pray Challenges Screen Content'),
      ),
    );
  }
}