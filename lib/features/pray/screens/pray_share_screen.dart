import 'package:flutter/material.dart';

class PrayShareScreen extends StatelessWidget {
  const PrayShareScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pray Share'),
      ),
      body: const Center(
        child: Text('Pray Share Screen Content'),
      ),
    );
  }
}